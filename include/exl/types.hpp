#pragma once

#define fn auto

using s8 = char;
using u8 = unsigned char;

using s16 = short int;
using u16 = unsigned short int;

using s32 = int;
using u32 = unsigned int;

using s64 = long int;
using u64 = unsigned long int;

using ssize = s64;
using usize = u64;